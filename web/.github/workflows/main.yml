name: FROSHIT CI

env:
  PG_PASSWORD: YznqXszFoRbKP9jO

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [ 16.x ]

    steps:
      - uses: actions/checkout@v3

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3

        with:
          node-version: ${{ matrix.node-version }}
      - run: |
          apt-get update
          apt-get install --yes --no-install-recommends postgresql-client
      - run: PGPASSWORD=$PG_PASSWORD psql -h db.mybvkrkmvnuzeqvzgbzg.supabase.co -p 5432 -d postgres -U postgres -f supabase/scripts/seed.sql
      - run: PGPASSWORD=$PG_PASSWORD psql -h db.mybvkrkmvnuzeqvzgbzg.supabase.co -p 5432 -d postgres -U postgres -f supabase/scripts/auth_fix.sql
      - run: PGPASSWORD=$PG_PASSWORD psql -h db.mybvkrkmvnuzeqvzgbzg.supabase.co -p 5432 -d postgres -U postgres -f supabase/scripts/auth_triggers.sql
